pre-commit:
  parallel: true
  commands:
    format:
      run: bun run format:check
    lint:
      run: bun run lint

commit-msg:
  commands:
    commitlint:
      run: bunx commitlint --edit {1}

pre-push:
  commands:
    check:
      run: bun run check
